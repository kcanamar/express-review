<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INDEX</title>
</head>
<body>
    <h1>INDEX PAGE</h1>
    <% console.log(data) %>

    <a href="/todos/new">CREATE A NEW TODO</a>

    <ul>
        <% data.forEach((todo) => { %>
            <li>
                <h2><%= todo.title %></h2>
                <p><%= todo.content %></p>
            </li>
    
        <% }) %>    
    </ul>
</body>
</html>